# Netlify config — proxy API to Render AUTH
[[redirects]]
  from = "/api/*"
  to   = "https://laser-sharks-auth.onrender.com/api/:splat"
  status = 200
  force  = true

# SPA fallback so deep links work
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
